# ADR 2026-05-29

Classes in package `primitives` that are meant to be instantiated must:
* have `__slots__` defined, and
* have their member types defined (available in `__annotations__`).

Par exemple:
```python
class Test:
	_model: nn.Module
	__slots__ = tuple(__annotations__)
```
